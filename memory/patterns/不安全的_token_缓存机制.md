# 模式: 不安全的 Token 缓存机制

## 代码特征
（自动从首次发现中提取，后续审查会逐步丰富）

## 标准修复
移除这个不安全的缓存机制。如果必须缓存，应使用带有 TTL（生存时间）的缓存，并且缓存 key 应使用完整的 Token 哈希值。更安全的做法是每次请求都验证 Token。

## 审查次数: 2

## 历史案例

### 案例 1
- **日期**: 2026-05-04_112647
- **来源 PR**: 第一次审查: 用户登录模块
- **文件**: demo/sample_pr.py:199-208
- **严重程度**: high
- **描述**: auth_require_permission 装饰器使用一个全局字典 _TOKEN_CACHE 来缓存 JWT 的解析结果。缓存 key 仅使用 Token 的后 20 位，容易产生碰撞。更重要的是，缓存没有过期时间，这意味着即使一个 JWT Token 被吊销或过期，其缓存的有效载荷仍然会被使用，导致权限提升或未授权访问。
- **建议**: 移除这个不安全的缓存机制。如果必须缓存，应使用带有 TTL（生存时间）的缓存，并且缓存 key 应使用完整的 Token 哈希值。更安全的做法是每次请求都验证 Token。

---
> 本文件由 Agent 自动维护，后续同类问题会自动追加案例。

### 案例 2
- **日期**: 2026-05-04_112825
- **来源 PR**: 第二次审查: 相同代码
- **文件**: demo/sample_pr.py:210-215
- **严重程度**: high
- **描述**: auth_require_permission 装饰器使用 Token 的后 20 位作为缓存 key，且缓存永不过期。这可能导致：1) Token 被吊销后仍可使用；2) 不同 Token 可能产生相同的缓存 key，导致权限混淆。
- **建议**: 1) 移除 Token 缓存，每次请求都验证签名；2) 如果必须缓存，使用完整的 Token 哈希作为 key，并设置合理的过期时间（如 5 分钟）。
