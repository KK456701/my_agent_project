---
name: "代码组织缺乏模块化，所有功能在单一文件中"
description: "整个 PR 将所有功能（数据库、认证、加密、缓存、业务逻辑）放在一个 300+ 行的文件中。虽然作为 demo 可以理解，但实际项目中应按照功能模块拆分到不同文件/包中，以提高可维护性和团队协作效率。"
---

## 历史案例

### 案例 1
- **日期**: 2026-05-04_112647
- **来源 PR**: 第一次审查: 用户登录模块
- **文件**: demo/sample_pr.py:1-301
- **描述**: 整个 PR 将所有功能（数据库、认证、加密、缓存、业务逻辑）放在一个 300+ 行的文件中。虽然作为 demo 可以理解，但实际项目中应按照功能模块拆分到不同文件/包中，以提高可维护性和团队协作效率。
- **修复**: 建议的目录结构：


### 案例 2
- **日期**: 2026-06-04 17381
- **来源 PR**: My feature prtest
- **文件**: src/main/java/com/study/room/utils/UserContext.java`:18-50
- **描述**: UserContext 类新增的 getTokenRemainingTime、generateToken、parseToken 三个方法，仅仅是直接调用了 JwtUtil 的对应静态方法，没有添加任何额外的业务逻辑、缓存、日志或上下文处理。这种纯委托方法增加了调用链长度（增加一次方法调用栈帧），在频繁调用场景下会带来微小的性能开销（方法调用、栈帧创建与销毁）。更重要的是，它增加了代码的维护成本和认
- **修复**: 建议直接删除 UserContext 中的这三个委托方法，让调用方直接使用 JwtUtil 的静态方法。如果确实需要封装（例如为了统一异常处理或添加日志），应在委托方法中添加实际价值，否则应遵循 KISS 原则保持简单。

### 案例 3
- **日期**: 2026-06-04 17384
- **来源 PR**: My feature prtest
- **文件**: src/main/java/com/study/room/utils/UserContext.java`:20-52
- **描述**: UserContext 中新增的 getTokenRemainingTime、generateToken、parseToken 三个方法（第20-52行）都是对 JwtUtil 同名方法的简单委托调用，没有增加任何额外的业务逻辑、缓存、日志或上下文处理。这创建了一个不必要的中间层，增加了调用栈深度（每次调用多一次方法压栈和出栈），虽然单次开销极小（纳秒级），但在高频调用场景下（如每个请求都调用）会
- **修复**: 1) 如果 UserContext 的目的是作为统一入口，建议在 UserContext 中直接实现核心逻辑，而非委托；2) 如果只是为了提供便捷方法，建议直接调用 JwtUtil 的方法，删除 UserContext 中的委托方法；3) 如果确实需要保留中间层，考虑在 UserContext 中添加缓存逻辑（如缓存解析后的 Claims 对象）来证明中间层的存在价值。

### 案例 4
- **日期**: 2026-06-04 17455
- **来源 PR**: My feature prtest
- **文件**: src/main/java/com/study/room/utils/UserContext.java`:22-24
- **描述**: UserContext.getTokenRemainingTime(token) 方法仅直接调用 JwtUtil.getRemainingTime(token)，没有添加任何额外逻辑。这创建了一个不必要的委托层，增加了调用链长度和维护成本。调用方可以直接使用 JwtUtil.getRemainingTime(token) 达到相同效果。
- **修复**: 删除 UserContext.getTokenRemainingTime 方法，让调用方直接使用 JwtUtil.getRemainingTime。如果确实需要封装，应添加额外业务逻辑。
