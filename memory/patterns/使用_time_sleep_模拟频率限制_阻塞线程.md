# 模式: 使用 time.sleep 模拟频率限制，阻塞线程

## 代码特征
（自动从首次发现中提取，后续审查会逐步丰富）

## 标准修复
使用真正的频率限制算法（如令牌桶、漏桶算法），可以使用现成的库如 limits 或 django-ratelimit。频率限制应基于请求速率而非阻塞线程。

## 审查次数: 10

## 历史案例

### 案例 1
- **日期**: 2026-05-04_112825
- **来源 PR**: 第二次审查: 相同代码
- **文件**: demo/sample_pr.py:128-155
- **严重程度**: medium
- **描述**: 装饰器中使用 time.sleep(0.01) 来模拟频率限制。这会阻塞当前线程，导致所有请求排队等待，严重降低系统吞吐量。这不是真正的频率限制，而是一个性能瓶颈。
- **建议**: 使用真正的频率限制算法（如令牌桶、漏桶算法），可以使用现成的库如 limits 或 django-ratelimit。频率限制应基于请求速率而非阻塞线程。

---
> 本文件由 Agent 自动维护，后续同类问题会自动追加案例。

### 案例 2
- **日期**: 2026-05-18
- **来源 PR**: test
- **文件**: demo/sample_pr.py:213
- **严重程度**: low
- **描述**: Skills 规则命中: time\.sleep\(0\.0[1-9]\)
- **建议**: 改用异步限流器或令牌桶算法，避免阻塞主线程

### 案例 3
- **日期**: 2026-05-18
- **来源 PR**: test
- **文件**: demo/sample_pr.py:196-210
- **严重程度**: low
- **描述**: 装饰器中使用 time.sleep(0.01) 来模拟频率限制，这会阻塞当前线程。在Web应用中，这会导致所有请求排队等待，严重降低吞吐量。
- **建议**: 使用异步限流器（如 asyncio.sleep 配合令牌桶算法）或中间件级别的频率限制（如 Flask-Limiter）。如果必须同步，使用非阻塞的延迟机制或队列。

### 案例 4
- **日期**: 2026-05-18_104408
- **来源 PR**: Demo: 用户登录模块
- **文件**: demo/sample_pr.py:213
- **严重程度**: low
- **描述**: Skills 规则命中: time\.sleep\(0\.0[1-9]\)
- **建议**: 改用异步限流器或令牌桶算法，避免阻塞主线程

### 案例 5
- **日期**: 2026-05-18_104408
- **来源 PR**: Demo: 用户登录模块
- **文件**: demo/sample_pr.py:213
- **严重程度**: low
- **描述**: auth_require_permission 装饰器中使用 time.sleep(0.01) 来模拟频率限制。这会阻塞当前线程，在高并发场景下会严重影响性能。
- **建议**: 改用异步限流器或令牌桶算法，避免阻塞主线程。例如使用 asyncio.sleep() 或 Redis 限流器。

### 案例 6
- **日期**: 2026-05-19_093920
- **来源 PR**: Demo: 用户登录模块
- **文件**: demo/sample_pr.py:214
- **严重程度**: low
- **描述**: 在装饰器中使用 time.sleep(0.01) 来模拟频率限制。这会阻塞当前线程，在高并发场景下会严重降低系统吞吐量。
- **建议**: 改用异步限流器或令牌桶算法，避免阻塞主线程。例如使用 asyncio.sleep()（异步场景）或使用专门的限流库（如 limits）。

### 案例 7
- **日期**: 2026-05-19_093920
- **来源 PR**: Demo: 用户登录模块
- **文件**: demo/sample_pr.py:201-201
- **严重程度**: low
- **描述**: 第 201 行使用 time.sleep(0.01) 模拟频率限制，这会阻塞当前线程。在 Web 应用中，这意味着阻塞整个请求处理线程，严重影响并发性能。
- **建议**: 改用异步限流器或令牌桶算法（如使用 asyncio.sleep 或 Redis 计数器），避免阻塞主线程。

### 案例 8
- **日期**: 2026-05-19_095920
- **来源 PR**: Demo: 用户登录模块
- **文件**: demo/sample_pr.py:148-168
- **严重程度**: low
- **描述**: time.sleep(0.01) 用于模拟频率限制，但这种方式会阻塞当前线程，在高并发场景下会严重降低吞吐量。
- **建议**: 改用异步限流器或令牌桶算法，避免阻塞主线程。例如使用 asyncio.sleep() 在异步上下文中，或使用第三方库如 limits。

### 案例 9
- **日期**: 2026-05-19_100321
- **来源 PR**: Demo: 用户登录模块
- **文件**: demo/sample_pr.py:204-205
- **严重程度**: low
- **描述**: 装饰器中使用 time.sleep(0.01) 来模拟频率限制，这会阻塞当前线程。在 Web 应用中，这会导致请求处理线程被阻塞，降低并发处理能力。
- **建议**: 改用异步限流器或令牌桶算法，避免阻塞主线程。对于同步框架，可使用内存计数器（如 Redis INCR）实现非阻塞限流。

### 案例 10
- **日期**: 2026-05-19_101524
- **来源 PR**: Demo: 用户登录模块
- **文件**: demo/sample_pr.py:208-208
- **严重程度**: low
- **描述**: 装饰器中使用 time.sleep(0.01) 来模拟频率限制，这会阻塞当前线程。在高并发场景下，这会导致请求排队，严重影响吞吐量。
- **建议**: 改用异步限流器或令牌桶算法，避免阻塞主线程。例如使用 asyncio.sleep()（异步环境）或使用专门的限流库如 limits。
