# 模式: 不必要的中间列表构建 — 可原地累加

## 代码特征
（自动从首次发现中提取，后续审查会逐步丰富）

## 标准修复
直接在循环中累加 total，省去中间列表：total = 0; for item in items: total += item['price'] * item['quantity'] * 0.15。或者使用 sum() 生成器表达式：total = sum(item['price'] * item['quantity'] * 0.15 for item in items)。

## 审查次数: 1

## 历史案例

### 案例 1
- **日期**: 2026-05-19_093920
- **来源 PR**: Demo: 用户登录模块
- **文件**: demo/sample_pr.py:88-100
- **严重程度**: medium
- **描述**: calculate_order_amount 函数先构建 prices 列表，再遍历求和。这浪费了 O(n) 的额外内存（n 为 items 数量）。对于大量订单项，可能导致不必要的内存分配和 GC 压力。
- **建议**: 直接在循环中累加 total，省去中间列表：total = 0; for item in items: total += item['price'] * item['quantity'] * 0.15。或者使用 sum() 生成器表达式：total = sum(item['price'] * item['quantity'] * 0.15 for item in items)。

---
> 本文件由 Agent 自动维护，后续同类问题会自动追加案例。
