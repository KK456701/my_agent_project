# 模式: 审计日志同步写入导致 I/O 瓶颈

## 代码特征
（自动从首次发现中提取，后续审查会逐步丰富）

## 标准修复
1. 使用异步日志写入（如 asyncio 或线程池），将 I/O 操作从请求处理路径中分离。2. 使用日志缓冲区批量写入（如每 100ms 或 1000 条日志刷一次盘），减少磁盘 I/O 次数。3. 考虑使用高性能日志系统（如 syslog-ng、fluentd）或消息队列（如 Kafka）异步处理审计日志。

## 审查次数: 1

## 历史案例

### 案例 1
- **日期**: 2026-05-18_105740
- **来源 PR**: stalemate_test.py
- **文件**: stalemate_test.py:69-80
- **严重程度**: medium
- **描述**: handle_api_request 在峰值 5000 QPS 下，每次请求同步写入 500 字节审计日志，产生 2.5MB/s 磁盘写入。同步 I/O 会阻塞请求处理线程，导致延迟增加和吞吐量下降。在磁盘成为瓶颈时，可能引发请求排队和超时。
- **建议**: 1. 使用异步日志写入（如 asyncio 或线程池），将 I/O 操作从请求处理路径中分离。2. 使用日志缓冲区批量写入（如每 100ms 或 1000 条日志刷一次盘），减少磁盘 I/O 次数。3. 考虑使用高性能日志系统（如 syslog-ng、fluentd）或消息队列（如 Kafka）异步处理审计日志。

---
> 本文件由 Agent 自动维护，后续同类问题会自动追加案例。
