# 模式: 认证装饰器违反单一职责原则

## 代码特征
（自动从首次发现中提取，后续审查会逐步丰富）

## 标准修复
将装饰器拆分为多个独立的装饰器或中间件，每个只负责一个职责。例如：@log_access @rate_limit @require_permission('admin') @validate_jwt。或者使用面向切面编程（AOP）的思想，将这些横切关注点分离。

## 审查次数: 2

## 历史案例

### 案例 1
- **日期**: 2026-05-19_095239
- **来源 PR**: Demo: 用户登录模块
- **文件**: demo/sample_pr.py:176-210
- **严重程度**: high
- **描述**: auth_require_permission 装饰器同时承担了 JWT 解析、权限校验、日志记录和频率限制四种职责。这导致装饰器难以理解、测试和修改。任何职责的变化都需要修改这个装饰器。
- **建议**: 将装饰器拆分为多个独立的装饰器或中间件，每个只负责一个职责。例如：@log_access @rate_limit @require_permission('admin') @validate_jwt。或者使用面向切面编程（AOP）的思想，将这些横切关注点分离。

---
> 本文件由 Agent 自动维护，后续同类问题会自动追加案例。

### 案例 2
- **日期**: 2026-05-19_101524
- **来源 PR**: Demo: 用户登录模块
- **文件**: demo/sample_pr.py:178-210
- **严重程度**: high
- **描述**: auth_require_permission 装饰器同时承担了 4 种职责：JWT 解析、权限校验、审计日志记录、频率限制。这违反了单一职责原则，导致：1) 难以测试（需要 mock 多个外部依赖）；2) 难以扩展（添加新功能需要修改现有代码）；3) 职责耦合（修改日志逻辑可能影响权限校验）。
- **建议**: 拆分为多个独立的装饰器或中间件：1) @jwt_required 负责 Token 解析和验证；2) @require_permission('admin') 负责权限校验；3) @audit_log 负责日志记录；4) @rate_limit 负责频率限制。每个装饰器只做一件事。
