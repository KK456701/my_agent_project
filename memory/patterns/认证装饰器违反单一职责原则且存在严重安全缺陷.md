# 模式: 认证装饰器违反单一职责原则且存在严重安全缺陷

## 代码特征
（自动从首次发现中提取，后续审查会逐步丰富）

## 标准修复
1) 拆分为多个装饰器：@jwt_required、@require_permission、@audit_log、@rate_limit；2) 使用 functools.lru_cache 或 Redis 替代手动缓存；3) 使用完整的 Token 哈希作为缓存键；4) 使用 logging 模块替代 print。

## 审查次数: 1

## 历史案例

### 案例 1
- **日期**: 2026-05-19_092955
- **来源 PR**: Demo: 用户登录模块
- **文件**: demo/sample_pr.py:163-199
- **严重程度**: critical
- **描述**: auth_require_permission 装饰器同时承担了 JWT 解析、权限校验、日志记录、频率限制四种职责，严重违反单一职责原则。更严重的是：1) 使用全局可变状态 _TOKEN_CACHE 作为缓存，线程不安全；2) 缓存键只用 Token 后 20 位，可能冲突；3) HMAC 校验形同虚设（结果未比较）；4) 使用 print 而非 logging。
- **建议**: 1) 拆分为多个装饰器：@jwt_required、@require_permission、@audit_log、@rate_limit；2) 使用 functools.lru_cache 或 Redis 替代手动缓存；3) 使用完整的 Token 哈希作为缓存键；4) 使用 logging 模块替代 print。

---
> 本文件由 Agent 自动维护，后续同类问题会自动追加案例。
