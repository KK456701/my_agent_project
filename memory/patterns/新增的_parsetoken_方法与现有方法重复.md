---
name: "新增的 parseToken 方法与现有方法重复"
description: "新增的 parseToken 方法（第32-42行）与文件中已有的同名方法功能完全重复，且新增方法后紧跟了一行无意义的注释 '//asdasd5466556'。这会导致代码冗余，增加维护成本，但不会对运行时性能产生直接影响。"
---

## 历史案例

### 案例 1
- **日期**: 2026-06-04_134037
- **来源 PR**: feat: 增加从token获取userId的便捷方法22222245a
- **文件**: src/main/java/com/study/room/utils/JwtUtil.java:32-42
- **描述**: 新增的 parseToken 方法（第32-42行）与文件中已有的同名方法功能完全重复，且新增方法后紧跟了一行无意义的注释 '//asdasd5466556'。这会导致代码冗余，增加维护成本，但不会对运行时性能产生直接影响。
- **修复**: 删除重复的 parseToken 方法以及无意义的注释行，保留原有方法即可。


### 案例 2
- **日期**: 2026-06-04 17202
- **来源 PR**: feat: 增加两个新方法
- **文件**: src/main/java/com/study/room/utils/JwtUtil.java`:54-84
- **描述**: 新增的 getUserIdFromToken 和 isTokenValid 方法在当前代码库中没有被任何调用方引用（代码图谱显示调用方为 '?'，表示未找到引用）。虽然这些方法可能是为未来使用而添加的，但当前提交中没有任何代码使用它们，存在成为死代码的风险。
- **修复**: 1) 如果这些方法是为后续 PR 准备的，建议在同一个 PR 中包含调用方代码，确保变更的完整性；2) 如果当前确实没有调用方，考虑移除这些方法，待需要时再添加。
