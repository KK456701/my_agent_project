# 模式: Token 缓存无过期策略 — 已吊销的 Token 仍可被使用

## 代码特征
（自动从首次发现中提取，后续审查会逐步丰富）

## 标准修复
1. 使用带 TTL 的缓存（如 cachetools.TTLCache）。2. 缓存键应包含 Token 的完整哈希。3. 对于高安全场景，每次请求都应验证 Token 的有效性，不缓存验证结果。

## 审查次数: 1

## 历史案例

### 案例 1
- **日期**: 2026-05-19_095920
- **来源 PR**: Demo: 用户登录模块
- **文件**: demo/sample_pr.py:197-206
- **严重程度**: medium
- **描述**: _TOKEN_CACHE 字典缓存了 Token 的验证结果，但没有设置过期时间。如果 Token 被吊销或用户权限变更，缓存中的旧数据仍会被使用，导致权限校验失效。
- **建议**: 1. 使用带 TTL 的缓存（如 cachetools.TTLCache）。2. 缓存键应包含 Token 的完整哈希。3. 对于高安全场景，每次请求都应验证 Token 的有效性，不缓存验证结果。

---
> 本文件由 Agent 自动维护，后续同类问题会自动追加案例。
