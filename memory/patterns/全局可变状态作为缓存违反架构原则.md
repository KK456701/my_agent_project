# 模式: 全局可变状态作为缓存违反架构原则

## 代码特征
（自动从首次发现中提取，后续审查会逐步丰富）

## 标准修复
1) 使用 functools.lru_cache 或 cachetools.TTLCache 替代手动字典缓存；2) 将缓存逻辑封装为独立的 CacheService 类，通过依赖注入使用；3) 考虑使用 Redis 等外部缓存服务。

## 审查次数: 1

## 历史案例

### 案例 1
- **日期**: 2026-05-19_101524
- **来源 PR**: Demo: 用户登录模块
- **文件**: demo/sample_pr.py:175-176
- **严重程度**: high
- **描述**: _TOKEN_CACHE 是模块级全局字典，被 auth_require_permission 装饰器内部使用。全局可变状态导致：1) 线程不安全，多线程环境下可能产生竞态条件；2) 测试困难，状态在测试用例间共享；3) 无法控制生命周期。
- **建议**: 1) 使用 functools.lru_cache 或 cachetools.TTLCache 替代手动字典缓存；2) 将缓存逻辑封装为独立的 CacheService 类，通过依赖注入使用；3) 考虑使用 Redis 等外部缓存服务。

---
> 本文件由 Agent 自动维护，后续同类问题会自动追加案例。
