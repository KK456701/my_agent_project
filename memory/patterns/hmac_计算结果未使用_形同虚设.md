# 模式: HMAC 计算结果未使用，形同虚设

## 代码特征
（自动从首次发现中提取，后续审查会逐步丰富）

## 标准修复
删除无用的 HMAC 计算代码，改用 PyJWT 库进行完整的签名验证。如果确实需要手动验证，应比较 expected 与 token 中的签名部分。

## 审查次数: 1

## 历史案例

### 案例 1
- **日期**: 2026-05-18
- **来源 PR**: test
- **文件**: demo/sample_pr.py:204
- **严重程度**: low
- **描述**: 第 204 行计算了 HMAC 签名，但结果赋值给变量 expected 后从未被使用。这导致签名验证完全失效，任何 token 都能通过验证。
- **建议**: 删除无用的 HMAC 计算代码，改用 PyJWT 库进行完整的签名验证。如果确实需要手动验证，应比较 expected 与 token 中的签名部分。

---
> 本文件由 Agent 自动维护，后续同类问题会自动追加案例。
