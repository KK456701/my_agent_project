# 模式: auth_require_permission 装饰器违反单一职责原则，承担四种职责

## 代码特征
（自动从首次发现中提取，后续审查会逐步丰富）

## 标准修复
拆分为多个独立的装饰器或中间件：1) @parse_jwt 负责 Token 解析；2) @require_permission('admin') 负责权限校验；3) @audit_log 负责日志；4) @rate_limit 负责频率限制。使用 Python 装饰器栈或中间件模式组合它们。

## 审查次数: 1

## 历史案例

### 案例 1
- **日期**: 2026-05-19_094200
- **来源 PR**: Demo: 用户登录模块
- **文件**: demo/sample_pr.py:148-162
- **严重程度**: high
- **描述**: 该装饰器同时负责：1) JWT 解析和验证；2) 权限校验；3) 审计日志记录；4) 频率限制。这种设计导致：职责耦合难以独立修改和测试；装饰器参数化复杂；无法复用单个功能（如单独使用权限校验而不做日志）。
- **建议**: 拆分为多个独立的装饰器或中间件：1) @parse_jwt 负责 Token 解析；2) @require_permission('admin') 负责权限校验；3) @audit_log 负责日志；4) @rate_limit 负责频率限制。使用 Python 装饰器栈或中间件模式组合它们。

---
> 本文件由 Agent 自动维护，后续同类问题会自动追加案例。
