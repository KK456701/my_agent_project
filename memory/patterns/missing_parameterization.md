---
name: "auth_require_permission 装饰器违反单一职责原则，承担四种职责"
description: "该装饰器同时负责：1) JWT 解析和验证；2) 权限校验；3) 审计日志记录；4) 频率限制。这种设计导致：职责耦合难以独立修改和测试；装饰器参数化复杂；无法复用单个功能（如单独使用权限校验而不做日志）。"
---

## 历史案例

### 案例 1
- **日期**: 2026-05-19_094200
- **来源 PR**: Demo: 用户登录模块
- **文件**: demo/sample_pr.py:148-162
- **描述**: 该装饰器同时负责：1) JWT 解析和验证；2) 权限校验；3) 审计日志记录；4) 频率限制。这种设计导致：职责耦合难以独立修改和测试；装饰器参数化复杂；无法复用单个功能（如单独使用权限校验而不做日志）。
- **修复**: 拆分为多个独立的装饰器或中间件：1) @parse_jwt 负责 Token 解析；2) @require_permission('admin') 负责权限校验；3) @audit_log 负责日志；4) @rate_limit 负责频率限制。使用 Python 装饰器栈或中间件模式组合它们。

### 案例 2
- **日期**: 2026-05-19_110600
- **来源 PR**: stalemate_test.py
- **文件**: stalemate_test.py:1-106
- **描述**: 该文件是一个对抗性冲突测试文件，旨在模拟安全与性能/架构之间的不可调和矛盾。文件中所有代码均为模拟场景的占位符，并非实际生产代码。例如：`old_crypto_lib` 是一个虚构的库，`_process_payment`、`_sync_blacklist` 等辅助函数均为空实现。因此，虽然代码中模拟了多个安全风险（如使用有CVE的库、缓存攻击窗口、日志泄露敏感信息、重复校验），但这些是故意设计的
- **修复**: 无需修复。该文件是测试/演示用途，用于评估多智能体系统在冲突场景下的决策能力。如果此文件被误用于生产环境，则需要根据每个冲突场景进行实际修复。

