# 模式: 缓存与数据库查询职责混合

## 代码特征
（自动从首次发现中提取，后续审查会逐步丰富）

## 标准修复
将缓存逻辑抽象为一个独立的 Cache 类或使用装饰器模式。例如，创建一个 @cached(ttl=300) 装饰器，将其应用于独立的 get_config_from_db 函数。这样缓存和数据库查询的职责就分离了。

## 审查次数: 1

## 历史案例

### 案例 1
- **日期**: 2026-05-04_112825
- **来源 PR**: 第二次审查: 相同代码
- **文件**: demo/sample_pr.py:210-230
- **严重程度**: medium
- **描述**: get_config_with_cache 函数同时处理了缓存逻辑和数据库查询逻辑。这违反了单一职责原则，使得函数难以测试（需要 mock 数据库和缓存）。
- **建议**: 将缓存逻辑抽象为一个独立的 Cache 类或使用装饰器模式。例如，创建一个 @cached(ttl=300) 装饰器，将其应用于独立的 get_config_from_db 函数。这样缓存和数据库查询的职责就分离了。

---
> 本文件由 Agent 自动维护，后续同类问题会自动追加案例。
