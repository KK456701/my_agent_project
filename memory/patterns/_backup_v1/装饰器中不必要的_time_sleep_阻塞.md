# 模式: 装饰器中不必要的 time.sleep 阻塞

## 代码特征
（自动从首次发现中提取，后续审查会逐步丰富）

## 标准修复
移除 time.sleep，改用真正的频率限制机制（如令牌桶算法、Redis 计数器）。如果只是模拟，应删除或替换为异步方式。

## 审查次数: 1

## 历史案例

### 案例 1
- **日期**: 2026-05-04_112825
- **来源 PR**: 第二次审查: 相同代码
- **文件**: demo/sample_pr.py:218-220
- **严重程度**: medium
- **描述**: auth_require_permission 装饰器中使用 time.sleep(0.01) 模拟频率限制，但这是同步阻塞操作，会阻塞整个请求处理线程。对于高并发场景，这会导致请求排队，吞吐量下降。
- **建议**: 移除 time.sleep，改用真正的频率限制机制（如令牌桶算法、Redis 计数器）。如果只是模拟，应删除或替换为异步方式。

---
> 本文件由 Agent 自动维护，后续同类问题会自动追加案例。
