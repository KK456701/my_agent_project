# 模式: 黑名单缓存导致欺诈检测延迟窗口

## 代码特征
（自动从首次发现中提取，后续审查会逐步丰富）

## 标准修复
采用分层缓存策略：1) 使用 Redis 等内存数据库存储黑名单，支持毫秒级查询（< 1ms）；2) 设置 TTL 为 30 秒而非 5 分钟；3) 对高风险交易（如大额转账）强制实时查询风控 API。如果 Redis 不可用，至少将同步间隔缩短到 30 秒以内。

## 审查次数: 1

## 历史案例

### 案例 1
- **日期**: 2026-05-18_105740
- **来源 PR**: stalemate_test.py
- **文件**: stalemate_test.py:39-50
- **严重程度**: high
- **描述**: is_fraudulent 函数使用内存缓存每 5 分钟同步一次黑名单。在这 5 分钟的窗口期内，新加入黑名单的欺诈用户仍可通过检测，导致欺诈交易被放行。虽然性能 SLA 要求 < 5ms，但安全时效性要求实时查询，当前设计存在明显的攻击窗口。
- **建议**: 采用分层缓存策略：1) 使用 Redis 等内存数据库存储黑名单，支持毫秒级查询（< 1ms）；2) 设置 TTL 为 30 秒而非 5 分钟；3) 对高风险交易（如大额转账）强制实时查询风控 API。如果 Redis 不可用，至少将同步间隔缩短到 30 秒以内。

---
> 本文件由 Agent 自动维护，后续同类问题会自动追加案例。
