# 模式: 过于激进的输入清洗可能导致功能异常

## 代码特征
（自动从首次发现中提取，后续审查会逐步丰富）

## 标准修复
根据上下文进行更精确的清洗。如果是为了防止 XSS，应使用专门的 HTML 转义库（如 html.escape）而不是直接删除字符。如果是为了防止 SQL 注入，应使用参数化查询，而不是依赖输入清洗。

## 审查次数: 2

## 历史案例

### 案例 1
- **日期**: 2026-05-19_095239
- **来源 PR**: Demo: 用户登录模块
- **文件**: demo/sample_pr.py:131-148
- **严重程度**: medium
- **描述**: re.sub(r'[<>"\']', '', value) 会移除所有尖括号、双引号和单引号。这可能导致用户输入中的合法字符被误杀，例如：用户名 "O'Brien" 会变成 "OBrien"，HTML 标签中的属性值无法正常显示。
- **建议**: 根据上下文进行更精确的清洗。如果是为了防止 XSS，应使用专门的 HTML 转义库（如 html.escape）而不是直接删除字符。如果是为了防止 SQL 注入，应使用参数化查询，而不是依赖输入清洗。

---
> 本文件由 Agent 自动维护，后续同类问题会自动追加案例。

### 案例 2
- **日期**: 2026-05-19_103513
- **来源 PR**: Demo: 用户登录模块
- **文件**: demo/sample_pr.py:128-155
- **严重程度**: medium
- **描述**: re.sub(r'[<>"\']', '', value) 会移除所有尖括号、双引号和单引号。这可能导致正常业务数据被破坏，例如用户姓名中包含 O'Brien 或产品描述中包含 2" x 4"。
- **建议**: 根据上下文进行更精确的清洗。如果是为了防止 XSS，应使用专门的 HTML 转义库（如 html.escape）而不是直接删除字符。如果是为了防止 SQL 注入，应使用参数化查询，而不是依赖输入清洗。
