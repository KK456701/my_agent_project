# 模式: 黑名单同步窗口期 — 5 分钟攻击窗口

## 代码特征
（自动从首次发现中提取，后续审查会逐步丰富）

## 标准修复
1. 使用增量同步：每次同步只拉取增量变更（如 last_sync 时间戳后的新增/删除），将同步耗时降至 <1ms。2. 使用 Redis 等内存数据库存储黑名单，支持 TTL 和实时更新，查询延迟 <1ms。3. 若必须缓存，将同步间隔缩短至 30-60 秒，并增加增量更新机制。

## 审查次数: 1

## 历史案例

### 案例 1
- **日期**: 2026-05-18_105740
- **来源 PR**: stalemate_test.py
- **文件**: stalemate_test.py:43-55
- **严重程度**: high
- **描述**: is_fraudulent 函数每 5 分钟同步一次黑名单，期间新增的欺诈用户不会被拦截。对于高频交易系统（500 万次/天），5 分钟窗口内可能发生大量欺诈交易。虽然实时查询（20-50ms）违反 5ms SLA，但当前设计存在严重安全时效问题。
- **建议**: 1. 使用增量同步：每次同步只拉取增量变更（如 last_sync 时间戳后的新增/删除），将同步耗时降至 <1ms。2. 使用 Redis 等内存数据库存储黑名单，支持 TTL 和实时更新，查询延迟 <1ms。3. 若必须缓存，将同步间隔缩短至 30-60 秒，并增加增量更新机制。

---
> 本文件由 Agent 自动维护，后续同类问题会自动追加案例。
