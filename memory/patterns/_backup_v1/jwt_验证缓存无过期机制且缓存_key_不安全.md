# 模式: JWT 验证缓存无过期机制且缓存 key 不安全

## 代码特征
（自动从首次发现中提取，后续审查会逐步丰富）

## 标准修复
1. 使用完整的 token 哈希作为缓存 key。2. 为缓存添加 TTL（如 5 分钟），或使用 Redis 等支持过期时间的缓存。3. 如果必须缓存，应同时缓存 token 的过期时间并验证。

## 审查次数: 2

## 历史案例

### 案例 1
- **日期**: 2026-05-04_112825
- **来源 PR**: 第二次审查: 相同代码
- **文件**: demo/sample_pr.py:196-210
- **严重程度**: high
- **描述**: auth_require_permission 装饰器中的 _TOKEN_CACHE 缓存了 JWT 解析结果，但：1. 缓存 key 只取 token 后 20 位，容易碰撞。2. 缓存永不过期，Token 被吊销后仍可使用。3. 每次请求都检查缓存，但缓存命中后跳过了 HMAC 验证，存在安全风险。
- **建议**: 1. 使用完整的 token 哈希作为缓存 key。2. 为缓存添加 TTL（如 5 分钟），或使用 Redis 等支持过期时间的缓存。3. 如果必须缓存，应同时缓存 token 的过期时间并验证。

---
> 本文件由 Agent 自动维护，后续同类问题会自动追加案例。

### 案例 2
- **日期**: 2026-05-18_104408
- **来源 PR**: Demo: 用户登录模块
- **文件**: demo/sample_pr.py:196-210
- **严重程度**: high
- **描述**: auth_require_permission 装饰器中的 _TOKEN_CACHE 缓存了 JWT 解析结果，但：1. 缓存 key 只取 token 后 20 位，可能导致不同 token 冲突。2. 缓存永不过期，token 被吊销后仍返回旧结果。3. 模块级可变字典在并发场景下存在竞态条件。
- **建议**: 1. 使用完整 token 的 SHA256 哈希作为缓存 key。2. 为缓存添加 TTL（如 5 分钟），或使用 Redis 等支持过期时间的缓存。3. 使用 functools.lru_cache 或线程安全的缓存方案。
