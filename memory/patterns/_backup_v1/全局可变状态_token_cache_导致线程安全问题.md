# 模式: 全局可变状态 _TOKEN_CACHE 导致线程安全问题

## 代码特征
（自动从首次发现中提取，后续审查会逐步丰富）

## 标准修复
1) 使用线程安全的缓存方案，如 functools.lru_cache（单线程）或 Redis（多进程/分布式）。2) 如果必须用内存缓存，使用 threading.Lock 保护写操作。3) 将缓存作为依赖注入，而非全局变量。

## 审查次数: 1

## 历史案例

### 案例 1
- **日期**: 2026-05-18
- **来源 PR**: test
- **文件**: demo/sample_pr.py:196-210
- **严重程度**: high
- **描述**: 装饰器内部使用了模块级全局字典 _TOKEN_CACHE 作为缓存。在多线程/多进程环境下，多个请求同时读写该字典会导致数据竞争、缓存污染甚至崩溃。此外，全局可变状态使得代码难以测试（测试间状态污染）且无法扩展为分布式缓存。
- **建议**: 1) 使用线程安全的缓存方案，如 functools.lru_cache（单线程）或 Redis（多进程/分布式）。2) 如果必须用内存缓存，使用 threading.Lock 保护写操作。3) 将缓存作为依赖注入，而非全局变量。

---
> 本文件由 Agent 自动维护，后续同类问题会自动追加案例。
