# 模式: 应用层逐行加密导致性能问题且架构不合理

## 代码特征
（自动从首次发现中提取，后续审查会逐步丰富）

## 标准修复
1. 考虑使用数据库内置的加密功能（如 SQLite 的加密扩展或 PostgreSQL 的 pgcrypto）。2. 如果必须在应用层加密，应批量处理：复用 cipher 对象，使用 CTR 模式避免填充。3. 将加密逻辑封装为独立的服务层，与数据访问层分离。

## 审查次数: 1

## 历史案例

### 案例 1
- **日期**: 2026-05-19_103513
- **来源 PR**: Demo: 用户登录模块
- **文件**: demo/sample_pr.py:240-270
- **严重程度**: high
- **描述**: save_user_data_encrypted 函数在应用层对每行数据单独创建 AES cipher 对象进行加密。对于 10000 行数据，需要 10000 次 AES 初始化，性能极差。同时，手动实现 PKCS7 填充和序列化容易出错。从架构角度看，应用层逐字段加密不如使用数据库透明加密（TDE）或列级加密。
- **建议**: 1. 考虑使用数据库内置的加密功能（如 SQLite 的加密扩展或 PostgreSQL 的 pgcrypto）。2. 如果必须在应用层加密，应批量处理：复用 cipher 对象，使用 CTR 模式避免填充。3. 将加密逻辑封装为独立的服务层，与数据访问层分离。

---
> 本文件由 Agent 自动维护，后续同类问题会自动追加案例。
