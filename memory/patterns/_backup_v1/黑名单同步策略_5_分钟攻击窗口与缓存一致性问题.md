# 模式: 黑名单同步策略 — 5 分钟攻击窗口与缓存一致性问题

## 代码特征
（自动从首次发现中提取，后续审查会逐步丰富）

## 标准修复
1. 实现 _sync_blacklist 函数，从风控系统增量拉取变更。2. 缩短同步间隔至 30-60 秒，或使用 WebSocket 推送实时更新。3. 考虑使用 Redis 缓存（TTL 60 秒）替代内存集合，既满足 <5ms 延迟，又能更快反映黑名单变更。

## 审查次数: 1

## 历史案例

### 案例 1
- **日期**: 2026-05-19_104435
- **来源 PR**: stalemate_test.py
- **文件**: stalemate_test.py:44-52
- **严重程度**: medium
- **描述**: is_fraudulent 函数使用内存黑名单，每 5 分钟同步一次。在同步间隔内，新增的欺诈用户 ID 不会被拦截，形成 5 分钟的攻击窗口。同时，_sync_blacklist 函数为空实现，实际未执行任何同步逻辑。
- **建议**: 1. 实现 _sync_blacklist 函数，从风控系统增量拉取变更。2. 缩短同步间隔至 30-60 秒，或使用 WebSocket 推送实时更新。3. 考虑使用 Redis 缓存（TTL 60 秒）替代内存集合，既满足 <5ms 延迟，又能更快反映黑名单变更。

---
> 本文件由 Agent 自动维护，后续同类问题会自动追加案例。
