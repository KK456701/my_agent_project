# 模式: 不必要的中间列表构建

## 代码特征
（自动从首次发现中提取，后续审查会逐步丰富）

## 标准修复
直接在循环中累加 total，省去中间列表的内存分配。修改为：total = 0; for item in items: total += item['price'] * item['quantity'] * 0.15; return total

## 审查次数: 4

## 历史案例

### 案例 1
- **日期**: 2026-05-18
- **来源 PR**: test
- **文件**: demo/sample_pr.py:88-101
- **严重程度**: medium
- **描述**: calculate_order_amount 函数先构建一个 prices 列表，然后再遍历该列表求和。这浪费了 O(n) 的内存空间，对于大量订单项（如 10000+）会造成不必要的内存压力。
- **建议**: 直接在循环中累加 total，省去中间列表的内存分配。修改为：total = 0; for item in items: total += item['price'] * item['quantity'] * 0.15; return total

---
> 本文件由 Agent 自动维护，后续同类问题会自动追加案例。

### 案例 2
- **日期**: 2026-05-18_104408
- **来源 PR**: Demo: 用户登录模块
- **文件**: demo/sample_pr.py:97-108
- **严重程度**: medium
- **描述**: calculate_order_amount 函数先构建 prices 列表，再遍历求和。这浪费了 O(n) 的内存，对于大量订单项会造成不必要的内存压力。
- **建议**: 直接在循环中累加 total，省去中间列表。修改为：total = 0; for item in items: total += item["price"] * item["quantity"] * 0.15

### 案例 3
- **日期**: 2026-05-19_100321
- **来源 PR**: Demo: 用户登录模块
- **文件**: demo/sample_pr.py:96-108
- **严重程度**: medium
- **描述**: calculate_order_amount 函数先构建 prices 列表，再遍历求和。这额外分配了 O(n) 的内存，对于大量 items 会造成不必要的 GC 压力。
- **建议**: 直接在循环中累加 total，省去中间列表：total = 0; for item in items: total += item['price'] * item['quantity'] * 0.15; return total。或者使用 sum() 生成器表达式：return sum(item['price'] * item['quantity'] * 0.15 for item in items)。

### 案例 4
- **日期**: 2026-05-19_101524
- **来源 PR**: Demo: 用户登录模块
- **文件**: demo/sample_pr.py:93-104
- **严重程度**: medium
- **描述**: calculate_order_amount 函数先构建 prices 列表，再遍历该列表求和。这浪费了 O(n) 的内存空间，对于大量 items 会显著增加内存压力。
- **建议**: 直接在循环中累加 total，省去中间列表：total = 0; for item in items: total += item['price'] * item['quantity'] * 0.15; return total。或者使用 sum() 和生成器表达式：return sum(item['price'] * item['quantity'] * 0.15 for item in items)。
