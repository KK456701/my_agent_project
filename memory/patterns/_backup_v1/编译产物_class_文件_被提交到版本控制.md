# 模式: 编译产物（.class 文件）被提交到版本控制

## 代码特征
（自动从首次发现中提取，后续审查会逐步丰富）

## 标准修复
删除新增的重复 parseToken 方法，保留原有的一个实现。如果原有方法存在缺陷，应直接修改原有方法而非新增一个。

## 审查次数: 1

## 历史案例

### 案例 1
- **日期**: 2026-06-04_134037
- **来源 PR**: feat: 增加从token获取userId的便捷方法22222245a
- **文件**: target/classes/...`:全部 | **来源**: 🤖Agent | **领域**: architecture
- **描述**: PR 中包含了大量 .class 文件的变更（约30个文件），这些是 Java 编译后的二进制产物。将编译产物提交到版本控制是严重的架构实践问题，会导致：1) 仓库体积膨胀，增加克隆和拉取时间；2) 产生大量无意义的 diff，干扰代码审查；3) 不同开发环境可能产生不同的编译结果，导致难以追踪的构建问题；4) 违反了基础设施即代码的原则，构建过程应该可重复且由 CI/CD 管理。
- **修复**: 1) 在 .gitignore 中添加 'target/' 目录；2) 从版本控制中移除所有 .class 文件；3) 确保构建过程通过 Maven/Gradle 等构建工具在 CI/CD 环境中完成，而非提交编译产物。

---
## 🟡 Medium

### 新增重复的 parseToken 方法导致代码冗余
- **位置**: `src/main/java/com/study/room/utils/JwtUtil.java:32-42
- **严重程度**: high
- **描述**: PR 中新增了一个 parseToken 方法（第32-42行），但根据 diff 上下文，该类中已经存在一个同名的 parseToken 方法（第45行之后）。新增的方法与原有方法功能完全一致，导致代码重复。这不仅违反了 DRY（Don't Repeat Yourself）原则，还会造成维护上的困惑——后续开发者不清楚应该使用哪个方法，且修改其中一个时容易遗漏另一个。
- **建议**: 删除新增的重复 parseToken 方法，保留原有的一个实现。如果原有方法存在缺陷，应直接修改原有方法而非新增一个。

---
> 本文件由 Agent 自动维护，后续同类问题会自动追加案例。
