# 模式: 认证装饰器违反单一职责原则且存在安全缺陷

## 代码特征
（自动从首次发现中提取，后续审查会逐步丰富）

## 标准修复
1. 拆分为多个装饰器或中间件：@jwt_required @require_permission('admin') @audit_log @rate_limit。2. 使用成熟的 JWT 库（如 PyJWT）进行签名验证。3. 使用 logging 模块替代 print。4. 移除不安全的缓存或使用带 TTL 的安全缓存。

## 审查次数: 2

## 历史案例

### 案例 1
- **日期**: 2026-05-19_100321
- **来源 PR**: Demo: 用户登录模块
- **文件**: demo/sample_pr.py:171-210
- **严重程度**: high
- **描述**: auth_require_permission 装饰器同时承担了 JWT 解析、权限校验、日志记录、频率限制四种职责，违反了单一职责原则。此外，JWT 解析未验证签名（仅用 split 解析 payload），HMAC 校验形同虚设（结果未比较），缓存键设计不安全（仅用 token 后 20 位），且使用 print 而非 logging。
- **建议**: 1. 拆分为多个装饰器或中间件：@jwt_required @require_permission('admin') @audit_log @rate_limit。2. 使用成熟的 JWT 库（如 PyJWT）进行签名验证。3. 使用 logging 模块替代 print。4. 移除不安全的缓存或使用带 TTL 的安全缓存。

---
> 本文件由 Agent 自动维护，后续同类问题会自动追加案例。

### 案例 2
- **日期**: 2026-05-19_101230
- **来源 PR**: Demo: 用户登录模块
- **文件**: demo/sample_pr.py:175-210
- **严重程度**: high
- **描述**: auth_require_permission 装饰器同时承担了 JWT 解析、权限校验、审计日志、频率限制四种职责，严重违反单一职责原则。此外，JWT 签名验证形同虚设（HMAC 结果未比较），使用 print() 而非 logging 记录审计日志，time.sleep() 模拟频率限制阻塞线程。
- **建议**: 1. 将装饰器拆分为多个独立装饰器或中间件：@jwt_required @require_permission @audit_log @rate_limit。2. 使用成熟的 JWT 库（如 PyJWT）进行签名验证。3. 使用 logging 模块替代 print。4. 使用异步限流器替代 time.sleep()。
