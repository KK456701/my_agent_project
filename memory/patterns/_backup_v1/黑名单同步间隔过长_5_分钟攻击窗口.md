# 模式: 黑名单同步间隔过长 — 5 分钟攻击窗口

## 代码特征
（自动从首次发现中提取，后续审查会逐步丰富）

## 标准修复
1. 将同步间隔缩短至 30-60 秒，或使用增量同步（仅同步变更）。2. 考虑使用 Redis 等内存数据库替代本地 set，支持实时更新且查询延迟 < 1ms。3. 如果必须遵守 5ms SLA，可采用双缓冲策略：主缓存用于查询（<0.1ms），后台线程每 30 秒原子切换。

## 审查次数: 2

## 历史案例

### 案例 1
- **日期**: 2026-05-19_110427
- **来源 PR**: stalemate_test.py
- **文件**: stalemate_test.py:40-50
- **严重程度**: high
- **描述**: _FRAUD_BLACKLIST 每 5 分钟同步一次，这意味着新加入黑名单的用户在最多 5 分钟内仍可通过欺诈检测。对于高频交易场景，5 分钟的攻击窗口可能导致大量欺诈交易通过。
- **建议**: 1. 将同步间隔缩短至 30-60 秒，或使用增量同步（仅同步变更）。2. 考虑使用 Redis 等内存数据库替代本地 set，支持实时更新且查询延迟 < 1ms。3. 如果必须遵守 5ms SLA，可采用双缓冲策略：主缓存用于查询（<0.1ms），后台线程每 30 秒原子切换。

---
> 本文件由 Agent 自动维护，后续同类问题会自动追加案例。

### 案例 2
- **日期**: 2026-05-19_110600
- **来源 PR**: stalemate_test.py
- **文件**: stalemate_test.py:44-52
- **严重程度**: medium
- **描述**: is_fraudulent 函数每 5 分钟同步一次黑名单，期间新增的欺诈用户 ID 不会被拦截。对于高频交易系统，5 分钟的攻击窗口可能导致大量欺诈交易通过。
- **建议**: 1. 使用增量同步机制：风控系统推送增量变更（Webhook/消息队列），实时更新内存黑名单。2. 如果必须轮询，将间隔缩短到 30-60 秒，并采用异步 I/O 避免阻塞主线程。3. 增加降级策略：当同步失败时，拒绝高风险交易（如大额、新用户）而非放行。
