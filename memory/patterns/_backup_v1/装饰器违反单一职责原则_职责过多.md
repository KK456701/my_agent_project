# 模式: 装饰器违反单一职责原则，职责过多

## 代码特征
（自动从首次发现中提取，后续审查会逐步丰富）

## 标准修复
将装饰器拆分为多个独立的装饰器或中间件：一个用于 JWT 解析和验证（如 @require_auth），一个用于权限校验（如 @require_permission('admin')），一个用于审计日志（如 @audit_log），一个用于频率限制（如 @rate_limit）。可以使用 Python 装饰器组合或 Web 框架的中间件机制来实现。

## 审查次数: 1

## 历史案例

### 案例 1
- **日期**: 2026-05-04_112825
- **来源 PR**: 第二次审查: 相同代码
- **文件**: demo/sample_pr.py:128-155
- **严重程度**: high
- **描述**: auth_require_permission 装饰器同时承担了 JWT 解析、权限校验、审计日志记录和频率限制（模拟）四种职责。这违反了单一职责原则，使得装饰器难以理解、测试和复用。任何职责的变化都需要修改这个装饰器。
- **建议**: 将装饰器拆分为多个独立的装饰器或中间件：一个用于 JWT 解析和验证（如 @require_auth），一个用于权限校验（如 @require_permission('admin')），一个用于审计日志（如 @audit_log），一个用于频率限制（如 @rate_limit）。可以使用 Python 装饰器组合或 Web 框架的中间件机制来实现。

---
> 本文件由 Agent 自动维护，后续同类问题会自动追加案例。
