# 模式: HMAC 签名验证形同虚设 — 计算结果未与预期值比较

## 代码特征
（自动从首次发现中提取，后续审查会逐步丰富）

## 标准修复
删除无用的 HMAC 计算代码，改用 PyJWT 库进行完整的 Token 验证。如果必须手动验证，应比较 expected 与 Token 中的签名部分。

## 审查次数: 1

## 历史案例

### 案例 1
- **日期**: 2026-05-19_095920
- **来源 PR**: Demo: 用户登录模块
- **文件**: demo/sample_pr.py:199-204
- **严重程度**: high
- **描述**: 代码计算了 HMAC 签名，但计算结果 expected 被赋值后从未使用。这导致签名验证完全失效，任何未签名的 Token 都会被接受。
- **建议**: 删除无用的 HMAC 计算代码，改用 PyJWT 库进行完整的 Token 验证。如果必须手动验证，应比较 expected 与 Token 中的签名部分。

---
> 本文件由 Agent 自动维护，后续同类问题会自动追加案例。
