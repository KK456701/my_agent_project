# 模式: 装饰器违反单一职责原则 — 承担4种职责

## 代码特征
（自动从首次发现中提取，后续审查会逐步丰富）

## 标准修复
拆分为多个装饰器或中间件链：@jwt_required（解析验证JWT）、@require_permission('admin')（权限校验）、@audit_log（审计日志）、@rate_limit（频率限制）。每个装饰器只做一件事，通过组合实现功能。

## 审查次数: 1

## 历史案例

### 案例 1
- **日期**: 2026-05-18
- **来源 PR**: test
- **文件**: demo/sample_pr.py:196-210
- **严重程度**: high
- **描述**: auth_require_permission 装饰器同时承担了JWT解析、权限校验、审计日志、频率限制四种职责。这违反了单一职责原则，导致：1) 任何职责的变更都需要修改装饰器；2) 无法单独测试某个职责；3) 代码耦合度高，难以扩展（如添加新的认证方式）。
- **建议**: 拆分为多个装饰器或中间件链：@jwt_required（解析验证JWT）、@require_permission('admin')（权限校验）、@audit_log（审计日志）、@rate_limit（频率限制）。每个装饰器只做一件事，通过组合实现功能。

---
> 本文件由 Agent 自动维护，后续同类问题会自动追加案例。
