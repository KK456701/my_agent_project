---
name: "全局配置缓存无过期策略"
description: "_CONFIG_CACHE 是一个全局字典，用于缓存配置项，但没有设置过期时间（TTL）和淘汰策略。如果配置在运行时被修改，缓存将返回过期的值。"
---

## 历史案例

### 案例 1
- **日期**: 2026-05-04_112825
- **来源 PR**: 第二次审查: 相同代码
- **文件**: demo/sample_pr.py:280-290
- **描述**: _CONFIG_CACHE 是一个全局字典，用于缓存配置项，但没有设置过期时间（TTL）和淘汰策略。如果配置在运行时被修改，缓存将返回过期的值。
- **修复**: 使用 functools.lru_cache 并设置 maxsize，或使用专门的缓存库（如 cachetools）并设置 TTL。

