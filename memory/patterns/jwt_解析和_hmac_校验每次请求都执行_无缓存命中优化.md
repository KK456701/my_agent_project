# 模式: JWT 解析和 HMAC 校验每次请求都执行 — 无缓存命中优化

## 代码特征
（自动从首次发现中提取，后续审查会逐步丰富）

## 标准修复
1. 使用成熟的 JWT 库（如 PyJWT）替代手动解析，库内部会做签名验证和缓存。2. 如果必须手动实现，使用完整的 Token 哈希作为缓存键，并设置 TTL（如 5 分钟）。3. 将 HMAC 验证结果与 Token 中的签名比较，而不是只计算不比较。

## 审查次数: 1

## 历史案例

### 案例 1
- **日期**: 2026-05-19_094200
- **来源 PR**: Demo: 用户登录模块
- **文件**: demo/sample_pr.py:196-210
- **严重程度**: high
- **描述**: auth_require_permission 装饰器中，每次请求都执行 JWT 解析（base64 解码 + JSON 解析）和 HMAC 签名计算。虽然代码尝试使用缓存（_TOKEN_CACHE），但缓存键设计不合理（只用后 20 位），且 HMAC 计算结果未与任何值比较，形同虚设。对于高频 API，每次请求都做这些操作会显著增加延迟。
- **建议**: 1. 使用成熟的 JWT 库（如 PyJWT）替代手动解析，库内部会做签名验证和缓存。2. 如果必须手动实现，使用完整的 Token 哈希作为缓存键，并设置 TTL（如 5 分钟）。3. 将 HMAC 验证结果与 Token 中的签名比较，而不是只计算不比较。

---
> 本文件由 Agent 自动维护，后续同类问题会自动追加案例。
