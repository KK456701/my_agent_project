# 模式: 装饰器使用 print() 做审计日志 — 违反日志规范

## 代码特征
（自动从首次发现中提取，后续审查会逐步丰富）

## 标准修复
使用 logging.getLogger(__name__).info() 替代 print()。配置日志处理器将审计日志输出到专门的文件或日志系统。

## 审查次数: 1

## 历史案例

### 案例 1
- **日期**: 2026-05-18
- **来源 PR**: test
- **文件**: demo/sample_pr.py:196-210
- **严重程度**: low
- **描述**: 装饰器中使用 print(f"[AUDIT] ...") 记录审计日志，而不是使用 logging 模块。这导致：1) 无法控制日志级别；2) 无法配置日志输出目标；3) 在生产环境中难以收集和分析审计日志。
- **建议**: 使用 logging.getLogger(__name__).info() 替代 print()。配置日志处理器将审计日志输出到专门的文件或日志系统。

---
> 本文件由 Agent 自动维护，后续同类问题会自动追加案例。
