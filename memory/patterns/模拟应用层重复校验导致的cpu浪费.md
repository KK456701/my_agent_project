---
name: "模拟应用层重复校验导致的CPU浪费"
description: "代码模拟了API Gateway已做输入校验后，应用层再次进行相同校验的场景，导致50%的CPU时间浪费在重复校验上。这是故意设计的冲突场景，用于展示安全深度防御原则与性能优化之间的矛盾。"
---

## 历史案例

### 案例 1
- **日期**: 2026-05-19_104435
- **来源 PR**: stalemate_test.py
- **文件**: stalemate_test.py:80-90
- **描述**: 代码模拟了API Gateway已做输入校验后，应用层再次进行相同校验的场景，导致50%的CPU时间浪费在重复校验上。这是故意设计的冲突场景，用于展示安全深度防御原则与性能优化之间的矛盾。
- **修复**: 在真实生产环境中，如果API Gateway的校验足够严格且可信，应用层可以只做关键业务校验，减少重复劳动。但此文件为测试用例，无需修改。

