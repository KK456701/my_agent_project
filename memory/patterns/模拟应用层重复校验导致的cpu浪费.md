# 模式: 模拟应用层重复校验导致的CPU浪费

## 代码特征
（自动从首次发现中提取，后续审查会逐步丰富）

## 标准修复
在真实生产环境中，如果API Gateway的校验足够严格且可信，应用层可以只做关键业务校验，减少重复劳动。但此文件为测试用例，无需修改。

## 审查次数: 1

## 历史案例

### 案例 1
- **日期**: 2026-05-19_104435
- **来源 PR**: stalemate_test.py
- **文件**: stalemate_test.py:80-90
- **严重程度**: info
- **描述**: 代码模拟了API Gateway已做输入校验后，应用层再次进行相同校验的场景，导致50%的CPU时间浪费在重复校验上。这是故意设计的冲突场景，用于展示安全深度防御原则与性能优化之间的矛盾。
- **建议**: 在真实生产环境中，如果API Gateway的校验足够严格且可信，应用层可以只做关键业务校验，减少重复劳动。但此文件为测试用例，无需修改。

---
> 本文件由 Agent 自动维护，后续同类问题会自动追加案例。
